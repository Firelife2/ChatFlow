javascript:(function(){    var frame = document.getElementById("example-frame");    if (frame) {        frame.parentNode.removeChild(frame);        return;    }    frame = document.createElement("iframe");    frame.src = "https://chatfl0w.glitch.me/";    frame.name = "example-frame";    frame.style.position = "fixed";    frame.style.top = "0";    frame.style.right = "0";    frame.style.width = "50%";    frame.style.height = "100%";    frame.style.border = "none";    frame.style.zIndex = "9999";    var closeButton = document.createElement("button");    closeButton.innerText = "X";    closeButton.style.position = "fixed";    closeButton.style.top = "0";    closeButton.style.right = "50%";    closeButton.style.zIndex = "9999";    closeButton.onclick = function() {        frame.parentNode.removeChild(frame);        closeButton.parentNode.removeChild(closeButton);    };    document.body.appendChild(frame);    document.body.appendChild(closeButton);    document.addEventListener("keydown", function(event) {        if (event.keyCode == 80 && !event.shiftKey && !event.altKey && !event.ctrlKey && !event.metaKey) {            if (frame) {                frame.parentNode.removeChild(frame);                closeButton.parentNode.removeChild(closeButton);            } else {                document.body.appendChild(frame);                document.body.appendChild(closeButton);            }        }    });})();
